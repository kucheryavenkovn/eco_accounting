
&НаКлиенте
Процедура ПервоначальноеЗаполнение(Команда)
	
	ЗаполнитьНаСервере();
	Элементы.Список.Обновить();
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьНаСервере()
	
	ИмяСправочника = СтрЗаменить(Список.ОсновнаяТаблица, "Справочник.", "");
	Справочники[ИмяСправочника].ПервоначальноеЗаполнениеСправочника(Истина);
	
КонецПроцедуры
