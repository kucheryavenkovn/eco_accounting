
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Вариант = 1;
	Параметры.Свойство("Вариант", Вариант);
	
	Строки = Новый Массив;
	Если Вариант = 1 Тогда
		Строки.Добавить("Подробнее о печати документов по шаблонам MS Word можно узнать ");
	ИначеЕсли Вариант = 2 Тогда
		Строки.Добавить("Подробнее о работе с планами размещения объектов недвижимости можно узнать ");
	КонецЕсли;
	Ссылка = "Подробнее";
	Строки.Добавить(Новый ФорматированнаяСтрока("здесь",,,, Ссылка)); 
	
	Пояснение = Новый ФорматированнаяСтрока(Строки);
	
КонецПроцедуры

&НаКлиенте
Процедура Подробнее(Команда)
	
	Закрыть(Вариант);
	 
КонецПроцедуры

&НаКлиенте
Процедура Реквизит1ОбработкаНавигационнойСсылки(Элемент, НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	Закрыть(Вариант);
	
КонецПроцедуры
